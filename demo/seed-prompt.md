# Seed prompt

Paste this into Codex from the repository root.

```text
$geo-citation-engineer

Query: best crm for startups
Brand: Acme
Competitor: HubSpot
Draft: demo/input/draft.md

This is a 2.5 minute demo. Do not open Python source.

1. Run `python demo.py` (Enter between beats) or `python demo.py --auto`.
2. Show the GAP stamp (Acme not cited, HubSpot cited), then the GEO page, then PASS.
3. Glance at demo/show.html on the second screen.
4. If Apify is slow, stay offline (default). Prefer `--judge gemini` when GEMINI_API_KEY is set.
```
